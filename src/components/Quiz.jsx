import { useEffect, useState } from 'react'
import { getQuiz } from '../services/getQuiz'

const Quiz = () => {
    //const [questionary, setQuestionary] = useState([])

    // useEffect(() => {
    //     //const quiz = getQuiz(5).then(result => setQuestionary(quiz));
    //     sessionStorage.setItem("quiz", quiz);
    // }, [])

    return <div>HOLA</div>

    // return (
    //     <div>
    //         {questionary.map( question => {
    //             return question;
    //         })}
    //     </div>
    // )
}

export default Quiz